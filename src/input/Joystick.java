package input;

//import com.studiohartman.jamepad.*;

public class Joystick {
/*	ControllerManager controllers;
	public boolean a, b, x, y, lb, rightBumper, menu, list, leftThumb, rightThumb;
	public double leftTrigger, rightTrigger, leftX, rightX, leftY, rightY;
	public int POV;

	public Joystick(){
		controllers = new ControllerManager();
		controllers.initSDLGamepad();
	}
	
	public void updateJoystick(){
		ControllerState state = controllers.getState(0);
		if(!state.isConnected) System.out.println("not Connected");
		a = state.a;
		b = state.b;
		x = state.x;
		y = state.y;
		lb = state.lb;
	}*/
}
